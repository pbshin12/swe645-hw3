# Peter Shin (G01073633)
# Helm template for the NodePort Service exposing the survey app pods.
apiVersion: v1
kind: Service
metadata:
  name: {{ include "swe645-hw3.fullname" . }}
  labels:
    {{- include "swe645-hw3.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      {{- if and (eq .Values.service.type "NodePort") .Values.service.nodePort }}
      nodePort: {{ .Values.service.nodePort }}
      {{- end }}
  selector:
    {{- include "swe645-hw3.selectorLabels" . | nindent 4 }}
